import { Hero } from '@/components/sections/Hero';

/**
 * Home.
 *
 * Fase 1 entrega o hero sobre a base de tokens e componentes. As demais
 * seções do roteiro — a subida, o diagnóstico de entrada, os territórios, o
 * método, as provas, as fundadoras, os conteúdos e a conversão final — entram
 * na fase 2, ainda estáticas, e só depois recebem motion.
 */
export default function HomePage() {
  return <Hero />;
}
